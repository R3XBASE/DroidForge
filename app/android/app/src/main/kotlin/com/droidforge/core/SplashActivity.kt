package com.droidforge.core

import android.os.Bundle
import io.flutter.embedding.android.FlutterActivity

/**
 * Main entry point for the DroidForge Flutter application.
 *
 * The MethodChannel is configured in [configureFlutterEngine] (inherited from this class
 * via the parent MainActivity).
 */
class SplashActivity : FlutterActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
    }
}
